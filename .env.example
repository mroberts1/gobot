# ============================================================
# Go Telegram Bot - Environment Variables
# ============================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
# ============================================================

# --- REQUIRED ---

# Telegram Bot (from @BotFather)
TELEGRAM_BOT_TOKEN=your_bot_token_here
TELEGRAM_USER_ID=your_telegram_user_id

# Claude Code CLI path (usually auto-detected)
# CLAUDE_PATH=claude

# Anthropic API key (required on headless VPS, optional on desktop)
# On desktop, Claude Code uses OAuth. On a VPS without a browser, set this:
# ANTHROPIC_API_KEY=sk-ant-api03-your_key_here

# --- REQUIRED: Supabase (persistent memory) ---

SUPABASE_URL=https://your-project.supabase.co
SUPABASE_ANON_KEY=your_anon_key
# Service role key gives full access (bypasses RLS) - recommended for server use
# SUPABASE_SERVICE_ROLE_KEY=your_service_role_key

# --- OPTIONAL: Fallback LLMs ---
# When Claude Code auth fails, these provide backup responses

# OpenRouter (cloud fallback - recommended)
# OPENROUTER_API_KEY=your_openrouter_key
# OPENROUTER_MODEL=moonshotai/kimi-k2.5

# Ollama (local fallback)
# OLLAMA_MODEL=qwen3-coder

# --- OPTIONAL: Voice ---
# ElevenLabs for text-to-speech and phone calls

# ELEVENLABS_API_KEY=your_key
# ELEVENLABS_VOICE_ID=your_voice_id
# ELEVENLABS_AGENT_ID=your_agent_id
# ELEVENLABS_PHONE_NUMBER_ID=your_phone_number_id
# USER_PHONE_NUMBER=+1234567890

# --- OPTIONAL: Transcription ---
# Gemini for voice message transcription

# GEMINI_API_KEY=your_gemini_key

# --- OPTIONAL: AI News ---
# Grok (xAI) for morning briefing AI news from X/Twitter

# XAI_API_KEY=your_xai_key

# --- OPTIONAL: Communication APIs ---
# Unipile for WhatsApp/LinkedIn in morning briefing

# UNIPILE_DSN=your_dsn
# UNIPILE_API_KEY=your_key
# UNIPILE_LINKEDIN_ACCOUNT_ID=your_account_id
# UNIPILE_WHATSAPP_ACCOUNT_ID=your_account_id

# --- OPTIONAL: Google Calendar & Gmail ---
# Calendar events and email checks use direct Google REST APIs.
# OAuth tokens are stored automatically in macOS Keychain when you
# set up the google-workspace or gmail-business MCP server.
# No env vars needed — see docs/architecture.md "Direct API vs Claude Subprocess"

# --- SYSTEM ---
# Health check server port (default: 3000)
# HEALTH_PORT=3000
